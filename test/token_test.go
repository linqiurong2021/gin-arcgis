package test

import (
	"testing"
)

func TestCreateToken(t *testing.T) {
	// var token = new(services.Token)
	// token.Name = "Hello"
	// token.ID = 1
	// // services.CreateToken(c, token)
	// tmpToken, err := services.CreateToken(token)
	// if err != nil {

	// 	assert.Equal(t, 1, 0)
	// }

	// redis.Set(context.Background(), "token", tmpToken)
	// fmt.Printf("\n###Token###:%s\n", tmpToken)
	// assert.Equal(t, 1, 1)

}
